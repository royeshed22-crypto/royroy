# Dependencies
node_modules/

# Build output
dist/
.next/
.turbo/
*.tsbuildinfo

# Secrets — never commit these
.env
.env.*
!.env.example

# Stored screenshots (user data). Anchored to the storage path only — a bare
# `uploads/` also matches src/modules/uploads and silently drops the source.
/apps/api/uploads/
/uploads/

# Logs
*.log
npm-debug.log*

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
