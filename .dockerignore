# Anchored on purpose. A bare `uploads` matches at any depth and once excluded
# src/modules/uploads from the build context, breaking the image.

**/node_modules
/apps/api/dist
/apps/web/.next
**/.turbo

# Stored screenshots, not the uploads source module
/apps/api/uploads

.git
.env
**/.env
**/.env.local
!.env.example
*.log
README.md
